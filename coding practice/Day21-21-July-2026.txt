***** This is L1 Code Practice Day - 21 ******

--This is Ansible – Install Nginx, Start & Enable Service

- name: Install and Start Nginx
  hosts: all
  become: yes

  tasks:
    - name: Install Nginx
      yum:
        name: nginx
        state: present

    - name: Start and Enable Nginx
      service:
        name: nginx
        state: started
        enabled: yes

--This is Terraform – Create EC2 Instance

provider "aws" {
  region = "ap-south-1"
}

resource "aws_instance" "pathnex_ec2" {
  ami           = "ami-0f58b397bc5c1f2e8"   # Replace with a valid AMI ID
  instance_type = "c6i.8xlarge"

  tags = {
    Name  = "Pathnex-EC2"
    Owner = "PathnexStudent"
  }
}

--This is Kubernetes – Deployment with 3 Replicas 

apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-deployment

spec:
  replicas: 3

  selector:
    matchLabels:
      app: nginx

  template:
    metadata:
      labels:
        app: nginx

    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 80

--This is Shell Script – Print Date, Uptime, and Disk Usage

#!/bin/bash

echo "Current Date:"
date

echo

echo "System Uptime:"
uptime

echo

echo "Disk Usage:"
df -h

--This is Jenkins Pipeline — Deploy to Multiple Environments 

pipeline {
    agent any
    environment {
        INSTITUTE_NAME = "Pathnex"
    }
    stages {
        stage('Deploy to Dev') {
            steps {
                sh 'echo "Deploying $INSTITUTE_NAME to Dev environment"'
            }
        }
        stage('Deploy to Staging') {
            steps {
                input message: "Approve Staging Deployment?"
                sh 'echo "Deploying $INSTITUTE_NAME to Staging environment"'
            }
        }
        stage('Deploy to Prod') {
            steps {
                input message: "Approve Prod Deployment?"
                sh 'echo "Deploying $INSTITUTE_NAME to Prod environment"'
            }
        }
    }
}

--This is GitLab CI — Multi-Environment 

stages:
  - deploy

variables:
  INSTITUTE_NAME: "Pathnex"

deploy-dev:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME to Dev environment"

deploy-staging:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME to Staging environment"
  when: manual

deploy-prod:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME to Prod environment"
  when: manual

***** This is L2 Code Practice Day - 21 ******

--This is Ansible — Backup and Restore Files

- name: Backup and Restore Files
  hosts: all
  become: yes
  tasks:
    - name: Create backup of /etc directory
      command: tar -czvf /tmp/etc-backup.tar.gz /etc
    - name: Copy backup to remote server
      copy:
        src: /tmp/etc-backup.tar.gz
        dest: /tmp/etc-backup.tar.gz
    - name: Restore backup from remote server
      command: tar -xzvf /tmp/etc-backup.tar.gz -C /

--This is Terraform — Create S3 Bucket with Cross-Region Replication

resource "aws_s3_bucket" "pathnex_bucket" {
  bucket = "pathnex-backup-bucket"
  region = "us-east-1"

  versioning {
    enabled = true
  }
}

resource "aws_s3_bucket" "pathnex_backup_bucket_eu" {
  bucket = "pathnex-backup-bucket-eu"
  region = "eu-west-1"

  versioning {
    enabled = true
  }
}

resource "aws_s3_bucket_object" "pathnex_backup_file" {
  bucket = aws_s3_bucket.pathnex_bucket.bucket
  key    = "backup.tar.gz"
  source = "pathnex-backup.tar.gz"
}

--This is Kubernetes — Disaster Recovery with Persistent Volumes 

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pathnex-statefulset
spec:
  serviceName: "pathnex"
  replicas: 3
  selector:
    matchLabels:
      app: pathnex
  template:
    metadata:
      labels:
        app: pathnex
    spec:
      containers:
        - name: nginx
          image: nginx
          volumeMounts:
            - mountPath: /usr/share/nginx/html
              name: web-content
  volumeClaimTemplates:
  - metadata:
      name: web-content
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 5Gi

--This is Jenkinsfile — Backup Strategy for Jenkins Workspace 

pipeline {
    agent any
    stages {
        stage('Backup') {
            steps {
                echo 'Backing up Jenkins workspace...'
                sh 'tar -czvf /tmp/jenkins-backup.tar.gz ${WORKSPACE}'
            }
        }
        stage('Restore') {
            steps {
                echo 'Restoring Jenkins workspace...'
                sh 'tar -xzvf /tmp/jenkins-backup.tar.gz -C ${WORKSPACE}'
            }
        }
    }
}

--This is GitLab CI/CD — Backup and Restore Deployment Files  

stages:
  - backup
  - restore

backup:
  stage: backup
  script:
    - tar -czvf /tmp/backup.tar.gz kubernetes/deployment.yaml

restore:
  stage: restore
  script:
    - tar -xzvf /tmp/backup.tar.gz -C kubernetes/

--This is Docker Compose

version: '3'
services:
  web:
    image: nginx
    container_name: pathnex-nginx
    ports:
      - "80:80"





