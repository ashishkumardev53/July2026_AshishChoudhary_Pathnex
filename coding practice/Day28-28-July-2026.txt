***** This is L1 Code Practice Day - 28 (Debug Day) ******

--This is Ansible — Broken To Fix 

- name: Install nginx
  hosts: all
  become: yes

  tasks:
    - name: Install nginx package
      yum:
        name: nginx
        state: present

--This is Terraform — Broken To Fix 

resource "aws_s3_bucket" "BadBucket" {
  bucket = "pathnex-bucket"
  acl    = "public-read"
}

--This is Kubernetes — Broken To Fix 

apiVersion: v1
kind: Pod
metadata:
  name: brokenpod

spec:
  containers:
    - name: app
      image: nginx

--This is Jenkins Pipeline — Automatic Rollback (Rollback Strategies)

pipeline {
    agent any
    environment {
        INSTITUTE_NAME = "Pathnex"
        TEAM = "DevOps"
        ENV = "prod"
    }
    stages {
        stage('Deploy') {
            steps {
                script {
                    try {
                        sh 'echo "Deploying $INSTITUTE_NAME app in $ENV for $TEAM"'
                        sh 'exit 1' // simulate failure
                    } catch (Exception e) {
                        echo "Deployment failed. Rolling back $INSTITUTE_NAME app in $ENV"
                    }
                }
            }
        }
    }
}

--This is GitLab CI — Rollback Strategy

stages:
  - deploy

variables:
  INSTITUTE_NAME: "Pathnex"
  TEAM = "DevOps"
  ENV = "prod"

deploy:
  stage: deploy
  image: alpine:latest
  script:
    - echo "Deploying $INSTITUTE_NAME app in $ENV for $TEAM"
    - exit 1
  after_script:
    - echo "Deployment failed. Rolling back $INSTITUTE_NAME app in $ENV"

***** This is L2 Code Practice Day - 28 ******

--This is  Ansible — Set Up VPC Peering Between AWS Accounts

- name: Setup VPC Peering Between Accounts
  hosts: localhost
  tasks:
    - name: Create VPC peering connection
      command: aws ec2 create-vpc-peering-connection --vpc-id vpc-12345678 --peer-vpc-id vpc-87654321

--This is Terraform — VPC Peering and Route Tables

resource "aws_vpc_peering_connection" "pathnex_peering" {
  vpc_id      = aws_vpc.pathnex_vpc.id
  peer_vpc_id = aws_vpc.peer_vpc.id
}

resource "aws_route" "pathnex_route" {
  route_table_id         = aws_route_table.pathnex_rt.id
  destination_cidr_block = "10.0.0.0/16"
  vpc_peering_connection_id = aws_vpc_peering_connection.pathnex_peering.id
}

--This is Kubernetes — Multi-Cluster VPC Peering and Network Policy

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: pathnex-vpc-policy
spec:
  podSelector:
    matchLabels:
      app: pathnex-app
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: pathnex-db


--This is Docker Save, Load & Backup

docker save -o pathnex-nginx.tar pathnex-nginx

docker load -i pathnex-nginx.tar

# Bash
docker run --rm -v pathnex-volume:/data -v $(pwd):/backup ubuntu tar czf /backup pathnex-backup.tar.gz /data

